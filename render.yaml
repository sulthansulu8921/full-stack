databases:
  - name: shopco-db
    plan: free
    ipAllowList: [] # Only allow internal connections

services:
  # 1. Django Backend
  - type: web
    name: shopco-backend
    runtime: python
    plan: free
    rootDir: shopco-backend
    buildCommand: "./build.sh"
    startCommand: "gunicorn backend.wsgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: shopco-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "*"

  # 2. React Frontend
  - type: web
    name: shopco-frontend
    runtime: static
    rootDir: shopco-frontend
    buildCommand: "npm run build"
    staticPublishPath: "build"
    envVars:
      - key: REACT_APP_API_URL
        value: "https://shopco-backend.onrender.com/"
